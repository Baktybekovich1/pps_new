# Overrides for the test environment. The database name gets the _test suffix
# from when@test in config/packages/doctrine.yaml.
APP_ENV=test
APP_SECRET=test-secret-not-used-outside-tests
KERNEL_CLASS='App\Kernel'
